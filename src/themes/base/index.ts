import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: 'Kode Mono, monospace',
  headings: {
    fontFamily: 'Kode Mono, monospace'
  },
  primaryColor: 'cyan',
  radius: {
    xs: '0px',
    sm: '0px',
    md: '0px',
    lg: '0px',
    xl: '0px',
  },
});
