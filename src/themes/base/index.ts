import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: 'Kode Mono, monospace',
  headings: {
    fontFamily: 'Kode Mono, monospace'
  },
  primaryColor: 'cyan',
  radius: {
    xs: '16px',
    sm: '32px',
    md: '42px',
    lg: '64px',
    xl: '92px',
  },
});
